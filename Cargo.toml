[package]
name = "sdf_marcher"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1
lto = "off"
debug-assertions = true

[profile.dev.package."*"]
opt-level = 3
debug-assertions = true

[dependencies]
bevy = {version = "0.14.0-rc.2", default-features = false, features = [
  "multi_threaded",
  "wayland",
  "bevy_color",
  "bevy_core_pipeline",
  "bevy_pbr",
  "bevy_render",
  "bevy_winit",
  "tonemapping_luts",
  "file_watcher",
  "bevy_sprite",
  "bevy_text",
  "default_font",

  # Formats
  "ktx2",
  "zstd",
]}
bevy_prototype_sdf = {path = "../bevy_prototype_sdf", default-features=false, features = ["bevy_asset"]}
